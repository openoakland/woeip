describe("login", () => {
    it.todo("should return a token on success");
    it.todo("should return an empty string on failure");
});

describe("logout", () => {
    it.todo("should return true on success");
    it.todo("should return false on failure");
})

describe("register", () => {
    it.todo("should return token on success");
    it.todo("should return empty string on failure");
})

describe("clearAuthToken", () => {
    it.todo("should remove auth token from local storage");
});

describe("getAuthTokenItem", () => {
    it.todo("should retrieve an auth token from local storage");
});

describe("setAuthTokenItem", () => {
    it.todo("should set the auth token in local storage");
});
